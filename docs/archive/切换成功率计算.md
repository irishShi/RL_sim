*Thinking...*

> **Analyzing Handover Success**
>
> I'm focused on the user's objective: a precise handover success rate derivation. I'm pinpointing key concepts and variables, essential for a correct formulation. The goal is to correct errors from the initial response, focusing on the framework's mathematical underpinnings. The task ahead feels clear.
>
> **Evaluating Corrected Variables**
>
> I'm now carefully examining the proposed variables for their precise roles in the handover success rate. My focus is on ensuring their consistent and correct application within the derivation's equations. I am critically analyzing the original paper's framework, identifying subtle errors that need fixing within the user's defined context. I'll need to double check the spatial coordinates.
>
> **Revising Probability Formulations**
>
> I'm now zeroing in on the handover trigger and execution probabilities. Correcting the spatial coordinates within the RSRP functions is key. I'm focusing on the position of the train over time and the impact on the received signal strength from both base stations, refining the probability calculations, and accounting for all the variables and their relations.
>
> **Refining the Probability Formula**
>
> I'm now carefully integrating the variables into the handover success rate equations. I've corrected the probability of handover trigger, ensuring the right spatial coordinates are used with the RSRP functions. I'm focusing on the integration limits and the dummy variables within the execution probability, all based on train position and speed.
>
> **Correcting the Handover Formulas**
>
> I'm now fully immersed in rectifying the handover probability equations, specifically focusing on the spatial coordinates within the RSRP functions. I'm verifying the handover trigger's mathematical representation and making sure the time-to-trigger (TTT) is applied correctly. The core issue of the previous response was the improper use of spatial coordinates.

基于对该论文核心思想的理解以及前文指出的数学表达瑕疵，我为您重新梳理并给出一个**严谨且物理意义正确**的越区切换成功率推导过程。

我们将推导分为四个主要步骤：物理场景与变量定义、切换触发概率推导、切换执行概率推导、以及最终的切换成功率综合。

### 1. 物理场景与基础变量定义

假设列车以速度 $v$ 从源基站 $a$ 驶向目标基站 $b$。

- **空间坐标**：设源基站 $a$ 的水平位置为 0，目标基站 $b$ 的水平位置为 $d_{ab}$。列车当前所在位置的坐标为 $x$。

- **多普勒频移**：根据论文的合理近似（忽略较小的天线高度差），多普勒频移 $f_d$ 与列车速度 $v$ 成正比：

  $f_d = \frac{f_c}{c} v \implies v = \frac{f_d c}{f_c}$

  其中 

  $f_c$

   为载波频率，

  $c$

   为光速。

- **接收信号强度 (RSRP)**：列车在位置 $x$ 处接收到基站 $a$ 和 $b$ 的信号强度分别为 $R(a, x)$ 和 $R(b, x)$。

------

### 2. 切换触发概率 $P_{handover}(x)$ 的修正推导

**物理逻辑**：
 根据 5G-R 的 A3 测量事件机制，切换触发必须同时满足两个条件：

1. **初始时刻（位置 $x$）**：目标基站信号比源基站信号强，且差值大于迟滞门限 $H_s$。
2. **触发时延结束时刻（位置 $x + vT_s$）**：列车以速度 $v$ 行驶了触发时延 $T_s$（即 Time-to-Trigger, TTT）后，上述条件依然满足。

**修正点**：论文原公式(18)中将目标基站的坐标写成了 $x - vT_s$，这是物理上的倒退。列车是向目标基站方向前进的，其绝对坐标应统一为 $x + vT_s$。

**正确推导**：
 设初始条件满足的概率为 $P_1$，时延结束后条件仍满足的概率为 $P_2$。

$P_1 = P(R(b, x) - R(a, x) \ge H_s)$

经过时间 $T_s$ 后，列车移动到了新位置 $x' = x + vT_s$。此时的概率 $P_2$ 为：

$P_2 = P(R(b, x + vT_s) - R(a, x + vT_s) \ge H_s)$

将速度 $v = \frac{f_d c}{f_c}$ 代入，得到**修正后的切换触发概率**：

$P_{handover}(x) = P_1 \cdot P_2 = P(R(b, x) - R(a, x) \ge H_s) \cdot P\left(R\left(b, x + \frac{f_d c}{f_c}T_s\right) - R\left(a, x + \frac{f_d c}{f_c}T_s\right) \ge H_s\right)$

------

### 3. 切换执行概率 $P_{exe}(x)$ 的修正推导

**物理逻辑**：
 当切换在位置 $x + vT_s$ 被成功触发后，系统开始执行切换（信令交互、资源分配等），这个过程需要耗时 $t_{exe}$。
 在这段执行时间内，列车继续向前行驶，移动区间为 $[x + vT_s, x + vT_s + vt_{exe}]$。为了保证切换不掉线，在这段**执行路径**上，目标基站 $b$ 的信号强度必须大于维持通信的最低阈值 $T$。

**修正点**：论文原公式(6)和(23)中存在积分变量与积分上下限混用的问题，且被积函数写成了常数。我们需要引入一个虚拟积分变量 $u$ 来表示列车在执行路径上的任意一点。

**正确推导**：
 切换执行概率是列车在执行路段上满足 $R(b, u) \ge T$ 的空间平均概率。
 执行路段的起点为 $x_{start} = x + vT_s$，终点为 $x_{end} = x + v(T_s + t_{exe})$，路段总长度为 $v t_{exe}$。

$P_{exe}(x) = \frac{1}{v t_{exe}} \int_{x + vT_s}^{x + v(T_s + t_{exe})} P(R(b, u) \ge T) du$

将速度 $v = \frac{f_d c}{f_c}$ 代入，得到**修正后的切换执行概率**：

$P_{exe}(x) = \frac{f_c}{f_d c t_{exe}} \int_{x + \frac{f_d c}{f_c}T_s}^{x + \frac{f_d c}{f_c}(T_s + t_{exe})} P(R(b, u) \ge T) du$

*(注：这里的 $u$ 是积分虚拟变量，代表列车在执行区间内的具体位置坐标)*

------

### 4. 最终切换成功率 $P_s(x)$ 的综合

**物理逻辑**：
 一次完整的越区切换成功，意味着它既被成功触发，又被成功执行。这两个过程在时间上是连续的，在概率上是相乘的关系。

**最终正确公式**：

$P_s(x) = P_{handover}(x) \cdot P_{exe}(x)$

展开后得到严谨的数学表达：

$\begin{aligned} P_s(x) =& \left[ P(R(b, x) - R(a, x) \ge H_s) \cdot P\left(R\left(b, x + \frac{f_d c}{f_c}T_s\right) - R\left(a, x + \frac{f_d c}{f_c}T_s\right) \ge H_s\right) \right] \\ &\times \left[ \frac{f_c}{f_d c t_{exe}} \int_{x + \frac{f_d c}{f_c}T_s}^{x + \frac{f_d c}{f_c}(T_s + t_{exe})} P(R(b, u) \ge T) du \right] \end{aligned}$

### 总结

通过引入虚拟积分变量 $u$ 并修正列车的绝对运动坐标 $x + vT_s$，上述推导完美契合了论文“将多普勒频移 $f_d$ 映射为空间距离，进而评估信号衰落概率”的核心思想，同时消除了原论文中的数学符号歧义和物理方向错误。在进行代码仿真（如 MATLAB）时，严格按照此修正后的公式进行离散化积分，即可得到准确的曲线。