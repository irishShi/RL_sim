# legacy_20260416_offline_rainbow

本目录收纳整理前散落在根目录、`checkpoints/`、`logs/` 和 `plots/` 下的历史实验产物。由于这些产物原本没有统一 run_id，本次按时间和内容归档为 legacy run。

后续重新训练或评估时，建议使用新的 `YYYYMMDD_HHMMSS_method_seedXX` 目录，并记录：

- 运行命令
- 使用的数据集和场景
- 配置文件快照
- 关键随机种子
- 主要指标和论文可用结论
